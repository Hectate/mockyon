# Vendored source

This directory is a vendored copy of [recoil-autohost](https://github.com/beyond-all-reason/recoil-autohost),
licensed under Apache-2.0 (see `LICENSE`/`AUTHORS`/`REUSE.toml`).

- Upstream: https://github.com/beyond-all-reason/recoil-autohost
- Vendored commit: `a27889fa9a7314fa3b97413b4cfbe5c6aa6ad305` (`main`, 2026-09-08)

To update, re-copy `src/`, `package.json`, `tsconfig.json`, `tsconfig.prod.json`, `LICENSE`, `AUTHORS`,
`REUSE.toml`, and `LICENSES/` from a newer upstream commit, update the commit SHA above, and re-run
`npm install` at the repo root.
